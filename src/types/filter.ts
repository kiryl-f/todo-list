export type Filter = 'all' | 'active' | 'completed';
export type SortBy = 'date' | 'priority';

export const FILTER_OPTIONS: Filter[] = ['all', 'active', 'completed'];
export const SORT_OPTIONS: SortBy[] = ['date', 'priority'];